# 作业问题简答

## 题 1：预约时间窗口规则

按以下顺序判断：

1. 任一端点缺失：`MISSING_BOUNDARY`
2. 结束时间不晚于开始时间：`END_NOT_AFTER_START`
3. 时长短于 30 分钟或长于 120 分钟：`DURATION_OUT_OF_RANGE`
4. 其它情况：`VALID`

边界示例：

- 30 分钟和 120 分钟均合法。
- 29 分钟和 121 分钟均超出范围。
- `09:00–09:29` 返回 `DURATION_OUT_OF_RANGE`。
- `09:00–09:00` 返回 `END_NOT_AFTER_START`，因为先检查结束时间顺序。

## 题 2：缺失预约的 HTTP 响应

**根因：** 查询按 `bookingId` 和 `roomId` 联合查找；没有匹配记录时，服务抛出 `BookingNotFoundException`。异常处理器已有 `BOOKING_NOT_FOUND` 错误码，但状态码误映射成了 500。

修正后返回 404，响应保持结构化 JSON，不包含异常类名或调用栈。

常见 4xx 状态码：

- `404 Not Found`：目标资源不存在。
- `400 Bad Request`：请求格式或参数无效，例如预约时间窗口错误。
- `409 Conflict`：请求与当前资源状态冲突，例如预约时段已被占用。

## 题 3：预约可用性判断

**根因：** 循环每次都用当前预约的重叠结果覆盖 `hasConflict`，后续不重叠预约会清掉之前发现的冲突。

改为累积逻辑 OR 后，只要任一预约重叠，冲突状态就会保留。

**海量关系型数据的优化思路：**

- 在数据库中用 `EXISTS` 查询是否存在重叠预约：`room_id` 相同，且 `start < 请求结束`、`end > 请求开始`。
- 为房间和时间列建立合适的索引，避免把该房间的全部预约加载到应用后逐条扫描。
- 创建预约时使用事务、锁或数据库约束，处理并发请求的竞争。

## 题 4：POST 与常见 HTTP 方法

- `POST`：向集合提交创建请求；服务端分配 ID，并用 201 和 `Location` 返回新资源地址。重复提交通常会创建多个资源，因此通常不具幂等性。
- `PUT`：对客户端已知地址的资源进行完整替换；相同请求重复执行应得到相同最终状态。
- `PATCH`：只修改资源的一部分；是否幂等取决于具体操作。
- `GET`：获取资源；应安全且幂等。
- `DELETE`：删除资源；重复执行后目标状态通常不变。
- `HEAD`：返回与 GET 相同的响应元数据，但不返回响应体。
- `OPTIONS`：查询资源支持的方法。
