# 作业问题简答

## 题 2：缺失预约的 HTTP 响应

根因：预约查询按 `bookingId` 和 `roomId` 联合查找；没有匹配记录时，服务抛出 `BookingNotFoundException`。异常处理器虽已使用 `BOOKING_NOT_FOUND` 错误码，却误将状态映射为 500。修正后返回 404，并保持错误响应为结构化 JSON，不返回异常类名或调用栈。

404 表示目标资源不存在；400 表示请求格式或参数无效（例如预约时间窗口错误）；409 表示请求与当前资源状态冲突（例如该时段已有预约）。

## 题 3：预约可用性判断

根因：遍历每条预约时直接将 `hasConflict` 赋为当前记录的重叠结果；前面的冲突会被后续不重叠记录覆盖。改为累积逻辑 OR 后，只要任一预约重叠就保持冲突。

海量关系型预约数据应把“存在重叠预约”的判断下推到数据库，例如按 `room_id`、`start < 请求结束`、`end > 请求开始` 做 `EXISTS` 查询，并为房间和时间列建立合适索引，避免将该房间全部预约加载到应用再逐条扫描。创建时还需用事务和锁或数据库约束处理并发预约竞争。
